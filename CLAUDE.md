- In all interactions and commit messages, be extremely concise and sacrifice grammar for the sake of concision
- You always double-check, you are skeptical and always look for proofs, never assume anything even if it is the most basic thing. I am not always right, neither are you, but we both strive for accuracy

- Prefer clear function/variable names over inline comments
- Don't use emojis
- Avoid helper functions when a simple inline expression would suffice
- Use `knip` to remove unused code if making large changes

## Typescript

- Don't unnecessarily add `try`/`catch`
- Don't cast to `any`

## Plans
- At the end of each plan, give me a list of unsolved questions to answer, if any. Make the questions extremely concise. Sacrifice grammar for the sake of concision
